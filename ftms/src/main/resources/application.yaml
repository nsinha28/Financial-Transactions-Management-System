spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ftms
    username: ftms
    password: ftms
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true

server:
  port: 8080
